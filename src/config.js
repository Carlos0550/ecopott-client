export const config = {
    apiBaseUrl: "https://ecopott-server.vercel.app"
    // process.env.NODE_ENV === "production"
    // ? "https://ecopott-server.vercel.app"
    // : "http://localhost:4000"
}